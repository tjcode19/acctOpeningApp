<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sleonegtb.accountopeningapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CaptureSignature"
            android:label="Signature Confirmation"
            android:theme="@android:style/Theme.Dialog" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Dashboard" />
        <activity android:name=".PersonalDet" />
        <activity android:name=".Uploads" />
        <activity android:name=".PhoneValidation" />
        <activity android:name=".CaptureSignatureActivity" />
        <activity android:name=".AccountHistory" />
        <activity android:name=".HistoryDetails" />
        <activity android:name=".Address"></activity>
    </application>

</manifest>